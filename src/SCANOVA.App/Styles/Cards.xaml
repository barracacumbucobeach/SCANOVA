<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Estilo de "card" de ação usado no Dashboard (seção 7) e reutilizável em outros módulos.
        Segue seção 8/148: cantos arredondados, espaçamento generoso, estado de hover/foco
        visíveis (herdados do estilo base de Button do WinUI 3), sem cores excessivas.
    -->
    <Style x:Key="ScanovaActionCardStyle" TargetType="Button">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Padding" Value="20" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="MinHeight" Value="132" />
    </Style>

</ResourceDictionary>
